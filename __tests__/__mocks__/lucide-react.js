module.exports = {
  // mock lucide-react icons to prevent Jest from failing
  ArrowUpRight: () => null,
  NotebookPen: () => null,
  SquarePen: () => null,
};

test("empty test just to pass", () => {});
